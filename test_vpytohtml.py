from vptohtml import *
from plumbum.path.utils import delete
from plumbum.cmd import ls, touch, mkdir

imports.js needs to be programmatically placed into the same directory as the target file.
and the 8081 server needs to be checked more reliably

then we need use SSstache to set up nonGlow files.